import styled from "styled-components";

const Sidebar = styled.div`
  color: #ffffff;
  & ul {
    padding-left: 0.5em;
  }
  & ul li {
    margin-bottom: 0.5em;
  }
  & a {
    font-size: 14px;
    color: #ffffff;
  }
`;

export default Sidebar;
